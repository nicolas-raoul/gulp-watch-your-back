`cd`  to your source folder, then run this script.

It will call `gulp deploy` whenever a change happen.

This is intended as a workaround for when `gulp watch` does not work reliably.

Pull requests very welcome! :-)
